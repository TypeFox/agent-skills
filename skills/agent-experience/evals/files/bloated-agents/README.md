# shiftplan

Builds staff rotas (round-robin over working days, skipping weekends and
company holidays) and calculates shift pay. The ops team uses it to draft
monthly schedules before manual review.

Python 3.11+, no third-party dependencies. Run the tests with `make test`.
