[project]
name = "shiftplan"
version = "0.5.0"
description = "Staff rota builder and shift pay calculator"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP"]

[tool.ruff.lint.isort]
force-single-line = false
